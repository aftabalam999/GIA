{
  "name": "gia-monorepo",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "ai:dev": "cd ai-service && PYTHONPATH=. .venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8001",
    "backend:dev": "npm --prefix backend run dev",
    "backend:test": "npm --prefix backend run test",
    "frontend:dev": "npm --prefix frontend run dev",
    "tauri:dev": "npm --prefix frontend run tauri dev",
    "dev": "concurrently -n \"backend,ai,tauri\" -c \"blue,yellow,green\" \"npm run backend:dev\" \"npm run ai:dev\" \"npm run tauri:dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run tauri build"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
